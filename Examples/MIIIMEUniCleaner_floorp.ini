; MIIIME Universal Cleaner Configuration
; ⓒ 2026 MIIIME

; 이 파일은 ANSI 인코딩으로 저장되어야 함.
; This file must be saved in ANSI encoding.

[Launch]
AppName=MIIIME Universal Cleaner

[Settings]
RunAsAdmin=0
ShowFinishMsg=1

[Options]
LogLevel=0

[Advanced]
RetryLimit=10
RetryDelay=50
LogRotationSize=5242880

[CleanupExclude]
01={Dat}\profile\places.sqlite
02={Dat}\profile\favicons.sqlite
03={Dat}\profile\cookies.sqlite
04={Dat}\profile\key4.db
05={Dat}\profile\logins.json
06={Dat}\profile\logins.db
07={Dat}\profile\cert9.db

; ── 설정 파일 ──
08={Dat}\profile\prefs.js
09={Dat}\profile\Invalidprefs.js
10={Dat}\profile\user.js
11={Dat}\profile\xulstore.json
12={Dat}\profile\handlers.json
13={Dat}\profile\containers.json
14={Dat}\profile\pkcs11.txt
15={Dat}\profile\permissions.sqlite

; ── 확장 프로그램 ──
16={Dat}\profile\extensions
17={Dat}\profile\extensions.json
18={Dat}\profile\extension-preferences.json
19={Dat}\profile\extension-settings.json
20={Dat}\profile\browser-extension-data
33={Dat}\profile\addons.json
21={Dat}\profile\addonStartup.json.lz4

; ── 검색 엔진 ──
22={Dat}\profile\search.json.mozlz4

; ── 세션 복원 (선택: 재시작 후 탭 복원이 필요한 경우 보존) ──
;23={Dat}\profile\sessionstore-backups
;24={Dat}\profile\sessionstore.jsonlz4

; ── 사이트별 데이터 (IndexedDB, localStorage 등) ──
; 보존 시 용량이 매우 커질 수 있습니다. 필요한 경우에만 활성화하세요.
25={Dat}\profile\storage
26={Dat}\profile\webappsstore.sqlite

; ── 사용자 스타일 (userChrome.css, userContent.css) ──
27={Dat}\profile\chrome

; ── Floorp 전용 ──
28={Dat}\profile\floorp
;29={Dat}\profile\floorp-os

; ── activity-stream (홈 화면 추천 피드 캐시 — 삭제해도 자동 재생성) ──
; 피드 설정을 유지하려면 보존, 재생성 허용이면 주석 처리
30={Dat}\profile\activity-stream.discovery_stream.json

; ── 인증서 예외 (특정 사이트 인증서를 수동 허용한 경우 보존) ──
;31={Dat}\profile\cert_override.txt

; ── 언어 ──
32={Dat}\profile\settings

; ── 데이터베이스 무결성 ──
33={Dat}\profile\storage.sqlite
34={Dat}\profile\storage-sync-v2.sqlite

[CleanupDelete]
